---
name: Bug report
about: Something in le is wrong or crashed
labels: bug
---

**`le --version`:**

**OS and arch** (e.g. macOS 15 arm64, Ubuntu 24.04 amd64):

**What happened:**

**What you expected instead:**

**Steps to reproduce:**

**`le list --json` output**, if relevant (redact anything sensitive — command
lines and working directories can contain paths you don't want to share):

```json
```
